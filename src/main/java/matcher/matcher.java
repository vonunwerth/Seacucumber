package matcher;

public interface matcher {

    /*default List<Node> previousNodes(Node node) {
        return null;
    }
    default List<Node> successingNodes(Node node) {
        return null;
    }
    default List<Node> findeNodes(Point point) {
        Label lb = Label.label(point.getLabel());
        findNodes(lb);
        return null;
    }

    List<Node> matchingAlgorithm();
    */

}
